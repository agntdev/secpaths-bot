[
  {
    "name": "Enroll in path shows path choices",
    "steps": [
      {
        "send": { "callback": "enroll:start" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Choose a learning path to enroll in:"
            }
          }
        ]
      }
    ]
  }
]
