[
  {
    "name": "View progress shows empty state for new user",
    "steps": [
      {
        "send": { "callback": "progress:view" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "📊 No progress yet — you aren't enrolled in any paths.\n\nTap 📚 Browse paths to get started."
            }
          }
        ]
      }
    ]
  }
]
